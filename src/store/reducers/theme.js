const initialState = {
	primary: '#2291e6',
	primaryHover: '#2476b5',
	primaryText: '#fff',
	background: '#efefef',
	backgroundText: '#000'
};

const reducer = (state = initialState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};

export default reducer;
